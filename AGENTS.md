# 工作区规则

## 架构文档

- 默认不读取 `docs/architecture.md`。
- 任务涉及 `README.md`、`.github/workflows/`、`profile-*` 生成资源、`ghfast.top` 代理、缓存版本或项目整体架构时，按需读取该文档。
- 局部任务优先按标题或关键词定位，仅阅读相关章节。
- 仅在跨模块修改、调整工作流链路或进行整体架构决策时阅读全文。
